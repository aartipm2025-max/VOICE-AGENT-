# Phase 1
pytest

# Phase 2 — LLM integration (optional, falls back to keywords if not installed)
groq

# Phase 4 — Actual MCPs (Google APIs)
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
# Phase 5 — REST API
fastapi
uvicorn

# Chat UI
streamlit
